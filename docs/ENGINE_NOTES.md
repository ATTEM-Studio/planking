# PLANKING Engine Notes

- 실제 순위는 검색 결과 배열 순서를 사용합니다.
- N1/N2/N3 및 100점 변환값은 네이버 공식 점수가 아닙니다.
- 지역형 키워드는 사용자 현재 위치의 `distance`가 아니라 검색어 지역 엔티티 경계를 사용합니다.
- 현재 관측 calibration은 하단맛집, 하단삼겹살, 하단회식, 하단고기집을 포함합니다.
- 신규 place/query fallback은 `low` 신뢰도로 표시합니다.
