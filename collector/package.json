{
  "name": "planking-rank-collector",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/*.mjs"
  },
  "dependencies": {
    "playwright": "latest"
  }
}
